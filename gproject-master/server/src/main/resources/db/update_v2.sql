-- ============================================
-- 应急指挥平台数据库更新脚本 v2
-- 更新日期：2026-03-04
-- 更新内容：为dispatch_tasks表添加reject_reason字段
-- ============================================

-- --------------------------------------------
-- 1. 更新dispatch_tasks表，添加拒绝原因字段
-- --------------------------------------------
ALTER TABLE `dispatch_tasks` 
ADD COLUMN `reject_reason` VARCHAR(500) DEFAULT NULL COMMENT '拒绝原因' AFTER `status`;

-- --------------------------------------------
-- 2. 更新dispatch_tasks表的status字段注释
-- --------------------------------------------
ALTER TABLE `dispatch_tasks` 
MODIFY COLUMN `status` TINYINT DEFAULT 0 COMMENT '任务状态：0-待接受,1-已接受,2-已拒绝,3-已出发,4-已到达,5-处理中,6-已完成,7-需增援';

-- --------------------------------------------
-- 3. 验证更新结果
-- --------------------------------------------
-- 查看dispatch_tasks表结构
-- DESC `dispatch_tasks`;

-- --------------------------------------------
-- 4. 回滚脚本（如果需要回滚）
-- --------------------------------------------
-- 如果需要回滚，执行以下SQL：
-- 
-- ALTER TABLE `dispatch_tasks` 
-- DROP COLUMN `reject_reason`;